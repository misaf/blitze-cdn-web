# engines in package.json is advisory by default: npm prints a warning and
# installs anyway, and the failure resurfaces later as an unreadable build
# error. Next 16 needs Node >= 20.9, so refuse the install instead.
engine-strict=true
