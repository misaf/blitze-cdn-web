---
title: Ansible roles
verifiedAgainst: blitzecdn.edge 1.0.0
lastVerified: 2026-08-05
---

# Ansible role variables

Verified against **blitzecdn.edge 1.0.0** — each role's
`meta/argument_specs.yml` and `defaults/main.yml`. Roles validate these
at the start of every run, so an unsupported value fails before the host
is touched.

Reference roles by their fully qualified name, for example
`blitzecdn.edge.blitzecdn_nginx`. Set variables in the control plane's
`ansible/inventory/group_vars/blitzecdn_edges.yml`. Ansible only
auto-loads `group_vars` adjacent to the inventory file — a `group_vars/`
directory anywhere else is silently ignored.

## Applying role changes safely

- Defaults are role defaults unless the control plane emits a desired-state
  value with the same name.
- Firewall, SSH, certificate-path, resolver, and Nginx-template changes can
  interrupt management or serving traffic. Plan with a canary inventory first.
- Never set a required variable to an empty value unless its description
  explicitly permits it; the firewall source list intentionally fails closed.
- A role rejecting `blitzecdn_desired_state_version` indicates an incompatible
  controller/edge pair. Stop and consult the [compatibility matrix](/docs/reference).
- Verify role changes with Nginx configuration tests and external traffic/TLS
  probes, not only an Ansible `changed=0` result.

## `blitzecdn_base`

Configure baseline edge packages and timezone.

| Variable | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `blitzecdn_base_packages` | `list` | `true` | `ca-certificates`, `curl` | — |
| `blitzecdn_base_timezone` | `str` | `true` | `UTC` | — |

## `blitzecdn_fail2ban`

Protect SSH using Fail2Ban.

| Variable | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `blitzecdn_fail2ban_ban_time` | `str` | `true` | `1h` | — |
| `blitzecdn_fail2ban_find_time` | `str` | `true` | `10m` | — |
| `blitzecdn_fail2ban_max_retry` | `int` | `true` | `5` | — |
| `blitzecdn_fail2ban_ssh_port` | `int` | `true` | `22` | — |

## `blitzecdn_firewall`

Configure a fail-closed UFW edge firewall.

| Variable | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `blitzecdn_firewall_enabled` | `bool` | `true` | `true` | — |
| `blitzecdn_firewall_http_ports` | `list` | `true` | `80`, `443` | — |
| `blitzecdn_firewall_ssh_port` | `int` | `true` | `22` | — |
| `blitzecdn_firewall_ssh_sources` | `list` | `true` | — | — |

## `blitzecdn_kernel`

Apply conservative edge kernel settings.

| Variable | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `blitzecdn_kernel_sysctl` | `dict` | `true` | `{'net.core.somaxconn': 16384, 'net.ipv4.tcp_syncookies': 1, 'net.ipv4.tcp_keepalive_time': 90, 'net.ipv4.tcp_keepalive_intvl': 30, 'net.ipv4.tcp_keepalive_probes': 5}` | — |

## `blitzecdn_nginx`

Converge Nginx CDN virtual hosts.

| Variable | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `blitzecdn_desired_state_version` | `int` | `true` | — | — |
| `blitzecdn_nginx_acme_root` | `path` | `true` | `/var/lib/blitzecdn/acme` | — |
| `blitzecdn_nginx_cache_inactive` | `str` | `true` | `60m` | — |
| `blitzecdn_nginx_cache_max_size` | `str` | `true` | `10g` | — |
| `blitzecdn_nginx_cache_path` | `path` | `true` | `/var/cache/nginx/blitzecdn` | — |
| `blitzecdn_nginx_cache_size` | `str` | `true` | `100m` | — |
| `blitzecdn_nginx_cache_zone` | `str` | `true` | `blitzecdn_cache` | — |
| `blitzecdn_nginx_certificate_path_pattern` | `str` | `true` | `^/(?!.*(?:^\|/)\.\.(?:/\|$))etc/(?:blitzecdn/tls\|ssl\|letsencrypt)/[^;{}]+$` | — |
| `blitzecdn_nginx_managed_sites_file` | `path` | `true` | `/etc/nginx/blitzecdn-managed-sites` | — |
| `blitzecdn_nginx_proxy_connect_timeout` | `str` | `true` | `5s` | — |
| `blitzecdn_nginx_proxy_read_timeout` | `str` | `true` | `30s` | — |
| `blitzecdn_nginx_resolver_timeout` | `str` | `true` | `5s` | — |
| `blitzecdn_nginx_resolver_valid` | `str` | `true` | `30s` | — |
| `blitzecdn_nginx_resolvers` | `list` | `true` | `127.0.0.53` | — |
| `blitzecdn_nginx_sites` | `list` | `true` | — | — |
| `blitzecdn_nginx_supported_state_versions` | `list` | `true` | `1` | — |

### `blitzecdn_nginx_sites` element keys

| Key | Type | Required | Default | Choices |
| --- | --- | --- | --- | --- |
| `cache_enabled` | `bool` | `false` | `true` | — |
| `cache_valid_not_found` | `str` | `false` | `1m` | — |
| `cache_valid_success` | `str` | `false` | `10m` | — |
| `certificate_key_path` | `path` | `false` | — | — |
| `certificate_key_source_path` | `path` | `false` | — | — |
| `certificate_mode` | `str` | `false` | `disabled` | `disabled`, `existing`, `uploaded`, `requested` |
| `certificate_path` | `path` | `false` | — | — |
| `certificate_source_path` | `path` | `false` | — | — |
| `enabled` | `bool` | `false` | `true` | — |
| `name` | `str` | `true` | — | — |
| `origin_host` | `str` | `true` | — | — |
| `origin_port` | `int` | `false` | — | — |
| `origin_request_host` | `str` | `false` | — | — |
| `origin_scheme` | `str` | `false` | `https` | `http`, `https` |
| `origin_sni` | `str` | `false` | — | — |
| `server_names` | `list` | `true` | — | — |
