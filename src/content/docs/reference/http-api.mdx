---
title: HTTP API reference
description: Authentication, endpoints, request schemas, responses, and errors for the control-plane HTTP API.
---

# HTTP API

Verified against the OpenAPI schema of `BlitzeCDN control plane`.

Every route below except `GET /health` requires the `X-API-Key` header.
Ten failed authentications from one client address within a minute return
`429`. Deployment and rollback routes return `202 Accepted` with a queued
record; poll `GET /v1/deployments/{deployment_id}` for the outcome.

## Request and retry rules

- Send `X-API-Key` on every `/v1/*` request. A missing or invalid key returns
  `401`; repeated failures can return `429` for the source address.
- Read-only `GET` requests are safe to retry with bounded backoff.
- Do not blindly retry `POST` mutations. A timeout can occur after the server
  committed a record mutation or queued a deployment. Query records, deployments,
  and audit events before deciding what to do next.
- `202` means queued, not converged. Poll the returned deployment ID until it is
  terminal and inspect retained output on any non-success status.
- The API has no client-supplied idempotency key.

Common error bodies use FastAPI validation details for `422`. Authentication,
conflict, and missing-resource responses contain a short `detail` string:

```json
{ "detail": "invalid API key" }
```

Never log the request header when capturing an error response.

The status code says whether retrying can help, so branch on it rather than on
the `detail` text:

| Status | Meaning                                                                               | Retry?                                                          |
| ------ | ------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `400`  | The request, or the controller's own configuration, is wrong                          | No — a person has to change something                           |
| `404`  | No such resource                                                                      | No                                                              |
| `409`  | Conflicts with current state                                                          | Only if the state changes; a `Retry-After` header means it will |
| `422`  | The request did not validate                                                          | No                                                              |
| `502`  | Something the controller drives — Ansible, certbot, an edge — did not answer usefully | Yes, with backoff                                               |
| `503`  | The controller cannot serve this right now                                            | Yes, with backoff                                               |

`409` with `Retry-After` is the deployment lock: another deployment, rollback,
upload, or issuance holds it, and it will be free again. Every other `409` needs
the request or the state changed first.

## Worked examples

Five requests covering the shapes the rest of the page describes in the
abstract. Every payload below is validated against the models the control plane
actually serves, so they can be copied and adapted rather than guessed at.

Responses are trimmed to the fields worth seeing; the model sections list the
rest.

### Publish a hostname

A record is the unit of configuration. Adding it with `proxied` set is what
derives an edge site — there is no separate site to create. On a proxied record
`value` is the origin the edge fetches from, not the address DNS should answer
with; see [Domains and records](/docs/reference/records).

{/* schema: DnsRecord */}

```bash
curl -sS -X POST https://controller.example.com/v1/domains/example.com/records \
  -H "X-API-Key: $BLITZE_API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{
    "domain": "example.com",
    "name": "www",
    "value": "198.51.100.20",
    "proxied": true,
    "origin_scheme": "https",
    "origin_request_host": "origin.internal.example.com",
    "cache_valid_success": "10m"
  }'
```

{/* schema: DnsRecord */}

```json
{
  "domain": "example.com",
  "name": "www",
  "type": "A",
  "value": "198.51.100.20",
  "ttl": 300,
  "proxied": true,
  "origin_scheme": "https",
  "origin_request_host": "origin.internal.example.com",
  "origin_port": null,
  "enabled": true,
  "certificate_mode": "disabled",
  "cache_enabled": true,
  "cache_valid_success": "10m",
  "cache_valid_not_found": "1m"
}
```

Nothing has reached an edge yet. The record is desired state until a deployment
converges it.

### Converge the fleet

[`POST /v1/deployments`](#post-v1deployments) returns `202` immediately; the
body is the queued record, not the outcome.

{/* schema: DeployRequest */}

```bash
curl -sS -X POST https://controller.example.com/v1/deployments \
  -H "X-API-Key: $BLITZE_API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{ "check": false }'
```

{/* schema: Deployment */}

```json
{
  "id": "7c1e4b90a2df46538ba0c7e15d93f8a2",
  "status": "queued",
  "operator": "ops",
  "check_mode": false,
  "host_limit": null,
  "rollback_of": null,
  "created_at": "2026-03-04T09:12:44.318271Z",
  "started_at": null,
  "finished_at": null,
  "result": null
}
```

### Poll it to a terminal state

Poll [`GET /v1/deployments/{deployment_id}`](#get-v1deploymentsdeployment_id)
until `status` leaves `queued` and `running`. `result.hosts` is the per-edge
breakdown, and `changed` on a non-check run counts tasks that acted.

```bash
curl -sS https://controller.example.com/v1/deployments/7c1e4b90a2df46538ba0c7e15d93f8a2 \
  -H "X-API-Key: $BLITZE_API_KEY"
```

{/* schema: Deployment */}

```json
{
  "id": "7c1e4b90a2df46538ba0c7e15d93f8a2",
  "status": "succeeded",
  "operator": "ops",
  "check_mode": false,
  "created_at": "2026-03-04T09:12:44.318271Z",
  "started_at": "2026-03-04T09:12:44.902118Z",
  "finished_at": "2026-03-04T09:14:07.551903Z",
  "result": {
    "id": "7c1e4b90a2df46538ba0c7e15d93f8a2",
    "playbook": "edge.yml",
    "status": "succeeded",
    "return_code": 0,
    "started_at": "2026-03-04T09:12:44.902118Z",
    "finished_at": "2026-03-04T09:14:07.551903Z",
    "hosts": [
      {
        "host": "edge-fra-1",
        "ok": 42,
        "changed": 3,
        "failed": 0,
        "unreachable": 0,
        "changes": [
          {
            "task": "nginx : write site configuration",
            "outcome": "changed"
          }
        ]
      }
    ]
  }
}
```

A `succeeded` deployment with `changed: 0` on every host means the fleet already
matched. That is the same signal [`POST /v1/drift`](#post-v1drift) produces,
without converging anything.

### Purge one path

Purges are partial by design: each entry names a host and a URI.

{/* schema: PurgeRequest */}

```bash
curl -sS -X POST https://controller.example.com/v1/cache/purge \
  -H "X-API-Key: $BLITZE_API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{
    "entries": [
      { "host": "www.example.com", "uri": "/assets/app.css", "scheme": "https" }
    ]
  }'
```

{/* schema: PurgeResult */}

```json
{
  "purged_at": "2026-03-04T09:20:11.004512Z",
  "entries": [
    { "host": "www.example.com", "uri": "/assets/app.css", "scheme": "https" }
  ],
  "purge_all": false,
  "complete": true,
  "failed_hosts": []
}
```

`complete` is the field to branch on. A partial purge returns `409` carrying
this same body with `complete` false and the unreachable edges in
`failed_hosts` — not an error object.

### Read certificate state

```bash
curl -sS https://controller.example.com/v1/sites/www.example.com/certificate \
  -H "X-API-Key: $BLITZE_API_KEY"
```

{/* schema: CertificateStatus */}

```json
{
  "site": "www.example.com",
  "source": "acme",
  "domains": ["www.example.com"],
  "not_after": "2026-06-02T09:14:00Z",
  "days_remaining": 90,
  "expired": false,
  "renewable": true,
  "fingerprint_sha256": "9f2c4e1a7b0d3856c9e4f01b2d7a58c36e91048fbd25a7c3019e6b4d8a52f7c1"
}
```

`renewable` is a property of the certificate's source, not of how soon it
expires: an uploaded certificate is never renewable by the controller.

## `GET /health`

Health

**Responses**

| Status | Description         | Body     |
| ------ | ------------------- | -------- |
| `200`  | Successful Response | `object` |

## `GET /v1/audit-events`

Audit Events

**Parameters**

| Name        | In       | Required | Type      | Description                   |
| ----------- | -------- | -------- | --------- | ----------------------------- |
| `limit`     | `query`  | `false`  | `integer` | —                             |
| `x-api-key` | `header` | `true`   | `string`  | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`AuditEvent[]`](#auditevent)        |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/workflows`

Lists durable deployment, rollback, and certificate workflows. A workflow in
`needs_review` was interrupted while an external system may already have acted;
inspect its completed steps before retrying.

| Name        | In       | Required | Type      | Description                   |
| ----------- | -------- | -------- | --------- | ----------------------------- |
| `limit`     | `query`  | `false`  | `integer` | Maximum workflows to return.  |
| `x-api-key` | `header` | `true`   | `string`  | Required operator credential. |

## `GET /v1/workflows/{workflow_id}`

Returns one durable workflow and its idempotent checkpoints.

| Name          | In       | Required | Type     | Description                   |
| ------------- | -------- | -------- | -------- | ----------------------------- |
| `workflow_id` | `path`   | `true`   | `string` | Workflow identifier.          |
| `x-api-key`   | `header` | `true`   | `string` | Required operator credential. |

## `GET /v1/deployments`

Deployments

**Parameters**

| Name        | In       | Required | Type      | Description                   |
| ----------- | -------- | -------- | --------- | ----------------------------- |
| `limit`     | `query`  | `false`  | `integer` | —                             |
| `x-api-key` | `header` | `true`   | `string`  | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`Deployment[]`](#deployment)        |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/deployments`

Queue a convergence; poll `GET /v1/deployments/{deployment_id}` for the outcome.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`DeployRequest`](#deployrequest)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `202`  | Successful Response | [`Deployment`](#deployment)          |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/deployments/{deployment_id}`

Deployment

**Parameters**

| Name            | In       | Required | Type     | Description                   |
| --------------- | -------- | -------- | -------- | ----------------------------- |
| `deployment_id` | `path`   | `true`   | `string` | —                             |
| `x-api-key`     | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`Deployment`](#deployment)          |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/drift`

Queue a check-mode run and read the answer from
`GET /v1/deployments/{deployment_id}/drift`. Converges nothing.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`DriftRequest`](#driftrequest)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `202`  | Successful Response | [`Deployment`](#deployment)          |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/deployments/{deployment_id}/drift`

Read any completed check-mode deployment as a drift report. Returns `409` for a
deployment that applied changes — its output describes what it did, not what had
drifted.

**Parameters**

| Name            | In       | Required | Type     | Description                   |
| --------------- | -------- | -------- | -------- | ----------------------------- |
| `deployment_id` | `path`   | `true`   | `string` | —                             |
| `x-api-key`     | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description                 | Body                  |
| ------ | --------------------------- | --------------------- |
| `200`  | Successful Response         | [`DriftReport`](#driftreport)         |
| `409`  | Not a check-mode deployment | `object`              |
| `422`  | Validation Error            | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/rollbacks`

Queue a rollback; poll `GET /v1/deployments/{deployment_id}` for the outcome.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`RollbackRequest`](#rollbackrequest)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `202`  | Successful Response | [`Deployment`](#deployment)          |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/sites`

List Sites. Sites are derived from proxied DNS records and are read-only —
there is no `POST`, `PATCH`, or `DELETE`. Change the record instead.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`CdnSite[]`](#cdnsite)           |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/sites/{name}`

The fully resolved policy for one site — the same document the edges are handed,
including the defaults its record never mentioned.

**Parameters**

| Name        | In       | Required | Type     | Description                        |
| ----------- | -------- | -------- | -------- | ---------------------------------- |
| `name`      | `path`   | `true`   | `string` | Site name, e.g. `cdn-example-com`. |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential.      |

**Responses**

| Status | Description          | Body                  |
| ------ | -------------------- | --------------------- |
| `200`  | Successful Response  | [`CdnSite`](#cdnsite)             |
| `404`  | No site by that name | `object`              |
| `422`  | Validation Error     | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/domains`

List Domains

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body       |
| ------ | ------------------- | ---------- |
| `200`  | Successful Response | [`Domain[]`](#domain) |

## `POST /v1/domains`

Create Domain

**Request body** (required): `application/json` → [`Domain`](#domain)

**Responses**

| Status | Description           | Body                  |
| ------ | --------------------- | --------------------- |
| `201`  | Successful Response   | [`Domain`](#domain)              |
| `409`  | Domain already exists | —                     |
| `422`  | Validation Error      | [`HTTPValidationError`](#httpvalidationerror) |

## `DELETE /v1/domains/{domain}`

Delete Domain, and every record in it.

**Responses**

| Status | Description           | Body |
| ------ | --------------------- | ---- |
| `204`  | Successful Response   | —    |
| `404`  | Domain does not exist | —    |

## `GET /v1/domains/{domain}/records`

List Records

**Responses**

| Status | Description           | Body          |
| ------ | --------------------- | ------------- |
| `200`  | Successful Response   | [`DnsRecord[]`](#dnsrecord) |
| `404`  | Domain does not exist | —             |

## `POST /v1/domains/{domain}/records`

Create Record. The body's `domain` must match the path segment.

**Request body** (required): `application/json` → [`DnsRecord`](#dnsrecord)

**Responses**

| Status | Description                                      | Body                  |
| ------ | ------------------------------------------------ | --------------------- |
| `201`  | Successful Response                              | [`DnsRecord`](#dnsrecord)           |
| `404`  | Domain does not exist                            | —                     |
| `409`  | Record exists, or its derived site name collides | —                     |
| `422`  | Validation Error                                 | [`HTTPValidationError`](#httpvalidationerror) |

## `PATCH /v1/domains/{domain}/records/{name}`

Update Record. This is the CDN on/off switch: send `{"proxied": true}` or
`{"proxied": false}`.

**Parameters**

| Name        | In       | Required | Type          | Description                   |
| ----------- | -------- | -------- | ------------- | ----------------------------- |
| `type`      | `query`  | `false`  | `A` \| `AAAA` | Record type; defaults to `A`. |
| `x-api-key` | `header` | `true`   | `string`      | Required operator credential. |

**Request body** (required): `application/json` → [`RecordPatch`](#recordpatch)

**Responses**

| Status | Description           | Body        |
| ------ | --------------------- | ----------- |
| `200`  | Successful Response   | [`DnsRecord`](#dnsrecord) |
| `404`  | Record does not exist | —           |

## `DELETE /v1/domains/{domain}/records/{name}`

Delete Record

**Responses**

| Status | Description           | Body |
| ------ | --------------------- | ---- |
| `204`  | Successful Response   | —    |
| `404`  | Record does not exist | —    |

## `GET /v1/dns/export`

Every record, for the system that publishes DNS. Proxied records omit `value`:
they must resolve to an edge, and edge addressing belongs to that system.

**Responses**

| Status | Description         | Body       |
| ------ | ------------------- | ---------- |
| `200`  | Successful Response | `object[]` |

## `GET /v1/certificates`

Managed certificates against the clock, soonest expiry first.

**Parameters**

| Name          | In       | Required | Type              | Description                                                            |
| ------------- | -------- | -------- | ----------------- | ---------------------------------------------------------------------- |
| `expiring_in` | `query`  | `false`  | `integer \| null` | Return only certificates with at most this many days left. `0`–`3650`. |
| `x-api-key`   | `header` | `true`   | `string`          | Required operator credential.                                          |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`CertificateStatus[]`](#certificatestatus) |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/cache/purge`

Remove cached responses from the edges. Answers inline rather than queueing: a
purge is usually made under time pressure and the caller needs to know whether
every edge actually dropped the object.

Send either `entries` or `purge_all`, never both — emptying the cache and
removing named entries are different operations, and combining them hides which
one ran. Sending neither is a `409` rather than a successful purge of nothing.

Every variant of a URL is removed: nginx keys entries by request method and by
normalized content encoding, so one URL is several entries.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`PurgeRequest`](#purgerequest)

**Responses**

| Status | Description                                 | Body                  |
| ------ | ------------------------------------------- | --------------------- |
| `200`  | Purged on every edge in scope               | [`PurgeResult`](#purgeresult)         |
| `404`  | No enabled site serves a requested hostname | `object`              |
| `409`  | Nothing to purge or a contradictory request | `object`              |
| `409`  | Purged on some edges but not all            | [`PurgeResult`](#purgeresult)         |
| `422`  | Validation Error                            | [`HTTPValidationError`](#httpvalidationerror) |

A partial purge is a `409`, not a `200`. Some edges dropped the object and
others did not, so which response a client gets depends on which edge answers —
that must not read as success.

The partial `409` carries a full [`PurgeResult`](#purgeresult), not an error object. Branch on
`complete`, and retry the edges named in `failed_hosts`; `hosts` holds what each
edge reported. A request refused before it reached the fleet — nothing to purge,
or `entries` and `purge_all` together — is the other `409` and carries an
`object` with `detail`, because no purge was attempted and there is no result to
report.

## `POST /v1/cache/stats`

Collect cache effectiveness from the edges. A `POST` despite reading only
counters: it runs a playbook across the fleet, which is neither cacheable nor
safe to retry blindly the way `GET` promises.

Edges that reported nothing appear in `edges` with an `error` rather than being
omitted, so a fleet-wide number is never quietly computed from a subset.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`StatsRequest`](#statsrequest)

**Responses**

| Status | Description                 | Body                  |
| ------ | --------------------------- | --------------------- |
| `200`  | Successful Response         | [`CacheStatsReport`](#cachestatsreport)    |
| `422`  | Validation Error            | [`HTTPValidationError`](#httpvalidationerror) |
| `503`  | No edge reported statistics | `object`              |

## `POST /v1/certificates/renew`

Reissue ACME certificates close to expiry. Answers inline rather than queueing:
renewal changes no edge configuration, so there is nothing to converge and
nothing to poll. Returns `renewed`, `skipped`, and `failed` name lists.

Uploaded certificates are never renewed — they appear under `skipped`. Renewed
certificates reach the edges on the next deploy.

A sweep is bounded by `BLITZE_CERTIFICATE_RENEWAL_BUDGET_SECONDS` and runs on a
pool of its own, so a slow CA cannot hold a request worker indefinitely or stall
unrelated endpoints. The budget is checked between sites and never during one,
so a truncated sweep never abandons an issuance in flight. Sites it did not
reach appear under `skipped` naming the budget, and are picked up by the next
run — the same way a transient failure already is. Raise the budget, or narrow
the run with `sites`, if a sweep is regularly truncated.

The optional `sites` field narrows the run, for retrying one failure without
sending every other subscription back to a rate-limited CA. A name with no
managed certificate behind it answers `404` rather than an empty success, so an
automated caller cannot read a typo as "nothing was due". An empty list is a
`422`: it would otherwise mean "renew nothing" while reading as a filter.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`RenewRequest`](#renewrequest)

**Responses**

| Status | Description                                 | Body                  |
| ------ | ------------------------------------------- | --------------------- |
| `200`  | Successful Response                         | [`RenewalResult`](#renewalresult) |
| `404`  | No managed certificate for a requested site | `object`              |
| `422`  | Validation Error                            | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/certificates/reconcile`

Issue first certificates for every proxied site that is ready for one, then
deploy once to install them. The same operation the scheduler runs on
`BLITZE_CERTIFICATE_RECONCILE_INTERVAL_SECONDS`, exposed so it can also be asked
for after fixing whatever was blocking preflight.

Eligibility is re-checked under the deployment lock, so this is safe to call
while the scheduler is running: only one of them can still see a site as
unissued and contact the CA. A site whose preflight blocks appears under
`skipped` and never reaches the CA; one whose issuance failed appears under
`failed` and does not stop the others.

`deployment` is `null` when nothing was issued — reconciliation converges the
fleet only when it has something new to install.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body            |
| ------ | ------------------- | --------------- |
| `200`  | Successful Response | [`ReconciliationResult`](#reconciliationresult) |

## `GET /metrics`

Prometheus text format. Gauges read out of the database at scrape time rather
than counters held in memory, so there is nothing to reset on restart and no
second source of truth about what happened.

Authenticated like every other data route: the fleet size and which certificates
are close to expiry are not public.

| Metric | Meaning |
| ------ | ------- |
| `blitzecdn_edges` | Registered edge servers. |
| `blitzecdn_sites` | Derived virtual hosts. |
| `blitzecdn_certificates_expiring` | Managed certificates near expiry. |
| `blitzecdn_certificates_unrenewable` | Of those, the ones BlitzeCDN cannot reissue — uploaded rather than issued, so somebody has to be asked for a replacement. |
| `blitzecdn_deployments{status}` | Recent deployments by status. |

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body     |
| ------ | ------------------- | -------- |
| `200`  | Successful Response | `text/plain` |

## `GET /v1/edges`

List Edges. The fleet, and therefore the Ansible inventory: the `blitzecdn`
inventory plugin reads these same rows at the start of every run.

**Responses**

| Status | Description         | Body     |
| ------ | ------------------- | -------- |
| `200`  | Successful Response | [`Edge[]`](#edge) |

## `POST /v1/edges`

Add Edge. Registers a host; converges nothing and contacts nothing. The edge
joins the fleet on the next `POST /v1/deployments`.

`201` rather than `202`: this creates a record and returns having finished,
unlike the deployment routes.

**Request body** (required): `application/json` → [`Edge`](#edge)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `201`  | Successful Response | [`Edge`](#edge)                |
| `409`  | Edge already exists | —                     |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/edges/{name}`

Get Edge

**Responses**

| Status | Description         | Body   |
| ------ | ------------------- | ------ |
| `200`  | Successful Response | [`Edge`](#edge) |
| `404`  | Edge does not exist | —      |

## `PATCH /v1/edges/{name}`

Update Edge. Each field replaces its own value; unset fields are untouched.
`public_addresses` and `ssh_sources` replace their whole list, so sending `[]`
clears them — merging would make "remove the last entry" inexpressible.

The name is not patchable. Certificates, audit history and deployment host
limits all refer to it, so a rename would orphan them; [`EdgePatch`](#edgepatch) has no such
field and forbids extras, which makes it a `422` rather than a silently ignored
key. Remove the edge and add it again under the new name.

**Request body** (required): `application/json` → [`EdgePatch`](#edgepatch)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`Edge`](#edge)                |
| `404`  | Edge does not exist | —                     |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `DELETE /v1/edges/{name}`

Remove Edge. Tears the host down, then deregisters it — in that order. The
inventory is derived from these rows, so deregistering first would leave a host
no playbook can address again, still serving its last converged virtual hosts
and still holding their private keys.

Answers inline rather than queueing, like the purge and stats routes: one
playbook against one host. It is still bounded by `deployment_timeout_seconds`,
so set a client timeout to match.

Returns `200` with a body rather than `204`. A forced removal that tore nothing
down is a success by the caller's own instruction, and `hosts` is the only place
that distinction survives.

**Query parameters**

| Name           | Type      | Default | Description                                                                                                                                                          |
| -------------- | --------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `decommission` | `boolean` | `true`  | Strip BlitzeCDN configuration and TLS private keys from the host before deregistering it.                                                                            |
| `force`        | `boolean` | `false` | Deregister even if the teardown failed. For a host that no longer exists; on one that is merely unreachable this leaves its configuration and private keys in place. |

**Responses**

| Status | Description                                                        | Body                  |
| ------ | ------------------------------------------------------------------ | --------------------- |
| `200`  | Successful Response                                                | [`EdgeRemoval`](#edgeremoval)         |
| `404`  | Edge does not exist                                                | —                     |
| `502`  | Teardown failed and the edge was kept; retry, or pass `force=true` | —                     |
| `422`  | Validation Error                                                   | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/origins/check`

Ask the edges to connect to the origins they proxy to: resolve, connect, and for
an HTTPS origin complete a verified TLS handshake with the same SNI the edge
sends. Each edge answers for itself, so the response is per edge and per site.

A `POST` despite changing nothing, following `POST /v1/cache/stats`: it runs a
playbook across the fleet, which is neither cacheable nor safe to retry blindly
the way `GET` promises. Answers inline, bounded by
`BLITZE_DEPLOYMENT_TIMEOUT_SECONDS`, so set a matching client timeout.

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Body** — [`OriginCheckRequest`](#origincheckrequest)

**Responses**

| Status | Description         | Body            |
| ------ | ------------------- | --------------- |
| `200`  | Successful Response | [`OriginReport`](#originreport) |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/sites/{name}/certificate`

Certificate

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `name`      | `path`   | `true`   | `string` | —                             |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`CertificateInfo`](#certificateinfo)     |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/sites/{name}/certificate/request`

Request Certificate

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `name`      | `path`   | `true`   | `string` | —                             |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `application/json` → [`CertificateRequest`](#certificaterequest)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`CertificateInfo`](#certificateinfo)     |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `GET /v1/sites/{name}/certificate/preflight`

Certificate Preflight

Reports whether HTTP-01 could validate this site right now: that public DNS
answers with an edge address, that the virtual host is deployed, and that CAA
permits the CA, plus advisories on the origin and the record TTL. Contacts no CA
and changes nothing. The check names and severities are listed under
[`blitzecdn cert preflight`](/docs/reference/cli#blitzecdn-cert-preflight).

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `name`      | `path`   | `true`   | `string` | —                             |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`PreflightReport`](#preflightreport)     |
| `404`  | No such site        | [`HTTPValidationError`](#httpvalidationerror) |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## `POST /v1/sites/{name}/certificate/upload`

Upload Certificate

**Parameters**

| Name        | In       | Required | Type     | Description                   |
| ----------- | -------- | -------- | -------- | ----------------------------- |
| `name`      | `path`   | `true`   | `string` | —                             |
| `x-api-key` | `header` | `true`   | `string` | Required operator credential. |

**Request body** (required): `multipart/form-data` → [`Body_upload_certificate_v1_sites__name__certificate_upload_post`](#body_upload_certificate_v1_sites__name__certificate_upload_post)

**Responses**

| Status | Description         | Body                  |
| ------ | ------------------- | --------------------- |
| `200`  | Successful Response | [`CertificateInfo`](#certificateinfo)     |
| `422`  | Validation Error    | [`HTTPValidationError`](#httpvalidationerror) |

## Models

### `AuditEvent`

| Field           | Type       | Required | Default |
| --------------- | ---------- | -------- | ------- | --- |
| `action`        | `string`   | `true`   | —       |
| `created_at`    | `string`   | `true`   | —       |
| `details`       | `object`   | `false`  | —       |
| `id`            | `integer`  | `true`   | —       |
| `operator`      | `string`   | `true`   | —       |
| `resource_id`   | `string \\ | null`    | `false` | —   |
| `resource_type` | `string`   | `true`   | —       |

### `Body_upload_certificate_v1_sites__name__certificate_upload_post`

| Field         | Type     | Required | Default |
| ------------- | -------- | -------- | ------- |
| `certificate` | `string` | `true`   | —       |
| `private_key` | `string` | `true`   | —       |

### `CdnSite`

Validated, provider-independent desired state for one CDN virtual host.

| Field                   | Type              | Required | Default    |
| ----------------------- | ----------------- | -------- | ---------- | --- |
| `cache_enabled`         | `boolean`         | `false`  | `true`     |
| `cache_valid_not_found` | `string`          | `false`  | `1m`       |
| `cache_valid_success`   | `string`          | `false`  | `10m`      |
| `certificate_key_path`  | `string \\        | null`    | `false`    | —   |
| `certificate_mode`      | [`CertificateMode`](#certificatemode) | `false`  | `disabled` |
| `certificate_path`      | `string \\        | null`    | `false`    | —   |
| `enabled`               | `boolean`         | `false`  | `true`     |
| `firewall`              | [`SiteFirewall`](#sitefirewall)    | `false`  | `{}`       |
| `name`                  | `string`          | `true`   | —          |
| `origin_host`           | `string`          | `true`   | —          |
| `origin_port`           | `integer \\       | null`    | `false`    | —   |
| `origin_request_host`   | `string \\        | null`    | `false`    | —   |
| `origin_scheme`         | [`HttpScheme`](#httpscheme)      | `false`  | `https`    |
| `origin_sni`            | `string \\        | null`    | `false`    | —   |
| `server_names`          | `string[]`        | `true`   | —          |

### `CertificateInfo`

| Field                | Type                | Required | Default |
| -------------------- | ------------------- | -------- | ------- | --- |
| `domains`            | `string[]`          | `true`   | —       |
| `email`              | `string \\          | null`    | `false` | —   |
| `fingerprint_sha256` | `string`            | `true`   | —       |
| `not_after`          | `string`            | `true`   | —       |
| `not_before`         | `string`            | `true`   | —       |
| `site`               | `string`            | `true`   | —       |
| `source`             | [`CertificateSource`](#certificatesource) | `true`   | —       |

### `CertificateMode`

_None._

### `CertificateRequest`

| Field   | Type       | Required | Default |
| ------- | ---------- | -------- | ------- | --- |
| `email` | `string \\ | null`    | `false` | —   |

### `CertificateSource`

_None._

### `CertificateStatus`

A stored certificate seen against the clock. Derived on request rather than
stored, so `days_remaining` is always current.

| Field                | Type                | Required | Default |
| -------------------- | ------------------- | -------- | ------- |
| `days_remaining`     | `integer`           | `true`   | —       |
| `domains`            | `string[]`          | `true`   | —       |
| `expired`            | `boolean`           | `true`   | —       |
| `fingerprint_sha256` | `string`            | `true`   | —       |
| `not_after`          | `string`            | `true`   | —       |
| `renewable`          | `boolean`           | `true`   | —       |
| `site`               | `string`            | `true`   | —       |
| `source`             | [`CertificateSource`](#certificatesource) | `true`   | —       |

`renewable` is `true` only for an ACME certificate. An uploaded one must be
replaced by whoever supplied it.

### `DeployRequest`

| Field        | Type             | Required | Default |
| ------------ | ---------------- | -------- | ------- |
| `check`      | `boolean`        | `false`  | `false` |
| `host_limit` | `string \| null` | `false`  | —       |

`host_limit` narrows the run to some edges — a canary. Names or globs, comma
separated, matching `^[A-Za-z0-9_.*-]+(?:,[A-Za-z0-9_.*-]+)*$` and at most 512
characters. Anything else is a `422`. The value is resolved against the
inventory, so it can only narrow the fleet.

### `Deployment`

| Field         | Type               | Required | Default |
| ------------- | ------------------ | -------- | ------- | --- |
| `check_mode`  | `boolean`          | `true`   | —       |
| `created_at`  | `string`           | `true`   | —       |
| `finished_at` | `string \\         | null`    | `false` | —   |
| `id`          | `string`           | `true`   | —       |
| `operator`    | `string`           | `true`   | —       |
| `return_code` | `integer \\        | null`    | `false` | —   |
| `rollback_of` | `string \\         | null`    | `false` | —   |
| `started_at`  | `string \\         | null`    | `false` | —   |
| `status`      | [`DeploymentStatus`](#deploymentstatus) | `true`   | —       |
| `stderr`      | `string`           | `false`  | —       |
| `stdout`      | `string`           | `false`  | —       |

### `DeploymentStatus`

_None._

### `DriftReport`

Whether the fleet still matches the state the control plane declares.

| Field           | Type             | Required | Default |
| --------------- | ---------------- | -------- | ------- |
| `checked_at`    | `string`         | `true`   | —       |
| `deployment_id` | `string`         | `true`   | —       |
| `host_limit`    | `string \| null` | `false`  | —       |
| `hosts`         | [`HostRun[]`](#hostrun)      | `false`  | `[]`    |

A task the roles skip under check mode cannot be counted, so the report floors
rather than exactly measures the difference. An unreachable host is reported as
unreachable, not as drift.

### `DriftRequest`

| Field        | Type             | Required | Default |
| ------------ | ---------------- | -------- | ------- |
| `host_limit` | `string \| null` | `false`  | —       |

Same constraints as `DeployRequest.host_limit`.

### `Edge`

One edge server: how the controller reaches it, and what the world reaches. The
Ansible inventory is derived from these, so a field here is a field the
`blitzecdn` inventory plugin publishes.

| Field              | Type             | Required | Default  |
| ------------------ | ---------------- | -------- | -------- |
| `host`             | `string`         | `true`   | —        |
| `name`             | `string`         | `true`   | —        |
| `port`             | `integer`        | `false`  | `22`     |
| `private_key_file` | `string \| null` | `false`  | —        |
| `public_addresses` | `string[]`       | `false`  | `[]`     |
| `ssh_sources`      | `string[]`       | `false`  | `[]`     |
| `user`             | `string`         | `false`  | `deploy` |

`host` becomes `ansible_host`, `user` becomes `ansible_user`, `port` becomes
both `ansible_port` and `blitzecdn_firewall_ssh_port` — published from one field
so the firewall cannot close the port Ansible connects on.

`public_addresses` is what the world reaches, and is empty for the ordinary
single-homed case, where it falls back to `host`. Certificate preflight checks a
customer's hostname against these, so on a NAT'd edge they must be set.

`ssh_sources` are management CIDRs. Every edge is published the union of every
edge's sources, as a host variable — narrowing them per host would let an edge
registered before a second office existed refuse the operator sitting in it.

### `EdgePatch`

A partial update to an edge. Every field optional; unset means untouched. No
`name` field, deliberately — see `PATCH /v1/edges/{name}`.

| Field              | Type               | Required | Default |
| ------------------ | ------------------ | -------- | ------- |
| `host`             | `string \| null`   | `false`  | —       |
| `port`             | `integer \| null`  | `false`  | —       |
| `private_key_file` | `string \| null`   | `false`  | —       |
| `public_addresses` | `string[] \| null` | `false`  | —       |
| `ssh_sources`      | `string[] \| null` | `false`  | —       |
| `user`             | `string \| null`   | `false`  | —       |

### `EdgeRemoval`

What `DELETE /v1/edges/{name}` did. The teardown and the deregistration fail
independently, so the outcome of the first is reported rather than implied.

| Field            | Type        | Required | Default |
| ---------------- | ----------- | -------- | ------- |
| `decommissioned` | `boolean`   | `true`   | —       |
| `hosts`          | [`HostRun[]`](#hostrun) | `false`  | `[]`    |
| `name`           | `string`    | `true`   | —       |

`hosts` carries the same per-host shape `PurgeResult.hosts` does: the counters,
the tasks that changed, and the tasks that failed. It is empty when no edge
reported at all — which is what a forced removal of a host that no longer exists
looks like.

### `HTTPValidationError`

| Field    | Type                | Required | Default |
| -------- | ------------------- | -------- | ------- |
| `detail` | [`ValidationError[]`](#validationerror) | `false`  | —       |

### `RenewalResult`

The outcome of one renewal sweep, per site.

| Field     | Type       | Required | Default |
| --------- | ---------- | -------- | ------- |
| `failed`  | `string[]` | `false`  | `[]`    |
| `renewed` | `string[]` | `false`  | `[]`    |
| `skipped` | `string[]` | `false`  | `[]`    |

Three buckets rather than two, because "not renewed" has two very different
causes and only one needs a person. `failed` is a site the CA or preflight
refused. `skipped` is a site this run did not get to — the budget ran out, a
deployment held the lock, or the certificate was uploaded rather than issued and
cannot be reissued here. A skipped site is picked up by the next run, so a sweep
full of them is slower renewal rather than missed renewal.

### `ReconciliationResult`

First certificates issued for sites that were ready, and the deploy that
installed them.

| Field        | Type                          | Required | Default |
| ------------ | ----------------------------- | -------- | ------- |
| `deployment` | [`Deployment`](#deployment) \| `null` | `false` | — |
| `failed`     | `object`                      | `false`  | `{}`    |
| `issued`     | `string[]`                    | `false`  | `[]`    |
| `skipped`    | `object`                      | `false`  | `{}`    |

`skipped` and `failed` map a site name to the reason. `deployment` is `null`
when nothing was issued.

### `OriginCheckRequest`

Which edges should answer. All of them, unless narrowed.

| Field        | Type             | Required | Default |
| ------------ | ---------------- | -------- | ------- |
| `host_limit` | `string \| null` | `false`  | —       |

### `OriginReport`

What the fleet says about the origins it proxies to.

| Field        | Type                | Required | Default |
| ------------ | ------------------- | -------- | ------- |
| `checked_at` | `string` (date-time) | `true`   | —       |
| `edges`      | [`EdgeOriginChecks[]`](#edgeoriginchecks) | `false` | `[]` |
| `host_limit` | `string \| null`    | `false`  | —       |

Read per site rather than per edge when deciding whether something is wrong: an
origin every edge can reach is healthy, one no edge can reach is down, and one
some edges can reach is a routing or allow-list problem that a single vantage
point could never have told apart.

### `EdgeOriginChecks`

One edge's answer about every origin it was asked to reach.

| Field        | Type                | Required | Default |
| ------------ | ------------------- | -------- | ------- |
| `checked_at` | `string \| null` (date-time) | `false` | — |
| `checks`     | [`OriginCheck[]`](#origincheck) | `false` | `[]` |
| `error`      | `string \| null`    | `false`  | —       |
| `host`       | `string`            | `true`   | —       |

`error` is set when this edge said nothing usable — unreachable, the role
failed, or it published no report. A silent edge is not a passing edge.

### `OriginCheck`

One edge's answer about one site's origin.

| Field          | Type              | Required | Default |
| -------------- | ----------------- | -------- | ------- |
| `detail`       | `string \| null`  | `false`  | —       |
| `origin`       | `string`          | `true`   | —       |
| `reachable`    | `boolean`         | `false`  | `false` |
| `scheme`       | [`HttpScheme`](#httpscheme)      | `true`   | —       |
| `site`         | `string`          | `true`   | —       |
| `sni`          | `string \| null`  | `false`  | —       |
| `status`       | `integer \| null` | `false`  | —       |
| `tls_verified` | `boolean \| null` | `false`  | —       |

Two separate answers because the fixes differ. `reachable` false means DNS,
routing, a firewall, or the wrong port — the edge got nothing back at all.
`tls_verified` false with `reachable` true means the origin answered but
presented a certificate that is not valid for the SNI this edge sends, which is
the site's `origin_sni` or the origin's own certificate and nothing to do with
the network. `tls_verified` is `null` for an HTTP origin, where the question
does not arise.

`status` is whatever the origin answered a `HEAD /` with, and is not judged: a
404 or a 403 still proves the origin is up and talking, which is the whole of
what this check claims to know.

### `HttpScheme`

_None._

### `RenewRequest`

| Field         | Type      | Required | Default |
| ------------- | --------- | -------- | ------- |
| `force`       | `boolean` | `false`  | `false` |
| `within_days` | `integer` | `false`  | `30`    |

`within_days` accepts `0`–`3650`. `force` renews every ACME certificate
regardless of age.

### `RollbackRequest`

| Field           | Type       | Required | Default |
| --------------- | ---------- | -------- | ------- | --- |
| `check`         | `boolean`  | `false`  | `false` |
| `deployment_id` | `string \\ | null`    | `false` | —   |

### `Domain`

| Field  | Type     | Required | Default |
| ------ | -------- | -------- | ------- |
| `name` | `string` | `true`   | —       |

### `DnsRecord`

| Field                   | Type              | Required | Default    |
| ----------------------- | ----------------- | -------- | ---------- |
| `cache_enabled`         | `boolean`         | `false`  | `true`     |
| `cache_valid_not_found` | `string`          | `false`  | `1m`       |
| `cache_valid_success`   | `string`          | `false`  | `10m`      |
| `certificate_key_path`  | `string \| null`  | `false`  | —          |
| `certificate_mode`      | [`CertificateMode`](#certificatemode) | `false`  | `disabled` |
| `certificate_path`      | `string \| null`  | `false`  | —          |
| `domain`                | `string`          | `true`   | —          |
| `enabled`               | `boolean`         | `false`  | `true`     |
| `firewall`              | [`SiteFirewall`](#sitefirewall)    | `false`  | `{}`       |
| `name`                  | `string`          | `true`   | —          |
| `origin_port`           | `integer \| null` | `false`  | —          |
| `origin_request_host`   | `string \| null`  | `false`  | —          |
| `origin_scheme`         | [`HttpScheme`](#httpscheme)      | `false`  | `https`    |
| `origin_sni`            | `string \| null`  | `false`  | —          |
| `proxied`               | `boolean`         | `false`  | `false`    |
| `ttl`                   | `integer`         | `false`  | `300`      |
| `type`                  | [`RecordType`](#recordtype)      | `false`  | `A`        |
| `value`                 | `string`          | `true`   | —          |

### `RecordPatch`

| Field                   | Type                      | Required | Default |
| ----------------------- | ------------------------- | -------- | ------- |
| `cache_enabled`         | `boolean \| null`         | `false`  | —       |
| `cache_valid_not_found` | `string \| null`          | `false`  | —       |
| `cache_valid_success`   | `string \| null`          | `false`  | —       |
| `certificate_key_path`  | `string \| null`          | `false`  | —       |
| `certificate_mode`      | `CertificateMode \| null` | `false`  | —       |
| `certificate_path`      | `string \| null`          | `false`  | —       |
| `enabled`               | `boolean \| null`         | `false`  | —       |
| `firewall`              | `SiteFirewall \| null`    | `false`  | —       |
| `origin_port`           | `integer \| null`         | `false`  | —       |
| `origin_request_host`   | `string \| null`          | `false`  | —       |
| `origin_scheme`         | `HttpScheme \| null`      | `false`  | —       |
| `origin_sni`            | `string \| null`          | `false`  | —       |
| `proxied`               | `boolean \| null`         | `false`  | —       |
| `ttl`                   | `integer \| null`         | `false`  | —       |
| `value`                 | `string \| null`          | `false`  | —       |

### `SiteFirewall`

Per-hostname request filtering applied at the edge. The posture is open: an
empty object serves everyone, and each list subtracts from that.

| Field               | Type       | Required | Default |
| ------------------- | ---------- | -------- | ------- |
| `allow_sources`     | `string[]` | `false`  | `[]`    |
| `allowed_countries` | `string[]` | `false`  | `[]`    |
| `denied_countries`  | `string[]` | `false`  | `[]`    |
| `denied_methods`    | `string[]` | `false`  | `[]`    |
| `denied_paths`      | `string[]` | `false`  | `[]`    |
| `deny_sources`      | `string[]` | `false`  | `[]`    |

`PATCH` replaces the whole object, so send every list you want to keep. Send
`{"firewall": {}}` to remove all rules. The `blitzecdn record firewall` CLI
command merges instead, keeping the lists you do not name.

Sources are IP addresses or CIDR networks with their host bits clear;
`203.0.113.5/24` returns `422`. Countries are ISO 3166-1 alpha-2 and require
GeoIP on the edge. See [the firewall
reference](/docs/reference/records) for how the rules compose and why
`/.well-known/acme-challenge/` is never filtered.

### `RecordType`

`A`, `AAAA`

### `ValidationError`

| Field   | Type       | Required   | Default |
| ------- | ---------- | ---------- | ------- | --- |
| `ctx`   | `object`   | `false`    | —       |
| `input` | `object`   | `false`    | —       |
| `loc`   | `string \\ | integer[]` | `true`  | —   |
| `msg`   | `string`   | `true`     | —       |
| `type`  | `string`   | `true`     | —       |

### `AnsibleRun`

One Ansible invocation. Required fields are `id`, `playbook`, `status`,
`started_at`, and `finished_at`. Optional fields are `return_code`, `log_path`,
and `error`; `hosts` defaults to `[]` and contains [`HostRun`](#hostrun) objects.

### `HostRun`

One edge's structured run result. `host` is required. The `ok`, `changed`,
`failed`, `unreachable`, `skipped`, `rescued`, and `ignored` counters default to
zero. `changes` and `failures` contain [`TaskResult`](#taskresult) objects; `report` carries an
operation-specific structured fact such as cache statistics.

### `RunStatus`

`succeeded`, `failed`, `timed_out`, `unstarted`

### `TaskResult`

| Field     | Type             | Required | Default |
| --------- | ---------------- | -------- | ------- |
| `task`    | `string`         | `true`   | —       |
| `action`  | `string`         | `false`  | `""`    |
| `outcome` | [`TaskOutcome`](#taskoutcome)    | `true`   | —       |
| `message` | `string \| null` | `false`  | —       |
| `role`    | `string \| null` | `false`  | —       |

### `TaskOutcome`

`ok`, `changed`, `failed`, `unreachable`, `skipped`

### `PurgeEntry`

| Field    | Type         | Required | Default |
| -------- | ------------ | -------- | ------- |
| `host`   | `string`     | `true`   | —       |
| `uri`    | `string`     | `true`   | —       |
| `scheme` | [`HttpScheme`](#httpscheme) | `false`  | `https` |

### `PurgeRequest`

`entries` is an array of at most 500 [`PurgeEntry`](#purgeentry) objects. `purge_all` defaults
to `false`; `host_limit` is an optional comma-separated edge pattern. Supply
entries or `purge_all`, never both or neither.

### `PurgeResult`

Contains `purged_at`, the requested `entries`, `purge_all`, `host_limit`, and
per-edge `hosts`. Read-only `complete` is true only when every edge in scope
purged; read-only `failed_hosts` names edges that may still serve the object.

### `StatsRequest`

`host_limit` is the only field and is optional.

### `CacheStatsReport`

Contains required `collected_at`, optional `host_limit`, and an `edges` array of
[`EdgeStats`](#edgestats) objects.

### `EdgeStats`

`host` is required. `collected_at`, `nginx_reachable`, connection counters,
per-site `sites`, and `error` describe one edge. A missing timestamp and an
error indicate that edge did not return a report.

### `SiteCacheStats`

Contains required `site` and an `outcomes` mapping of Nginx cache-status labels
to request counts.

### `PreflightReport`

Contains required `site` and a `checks` array of [`PreflightCheck`](#preflightcheck) objects.

### `PreflightCheck`

Contains required `name`, `passed`, `severity`, and human-readable `detail`.

### `PreflightSeverity`

`blocking`, `advisory`. A blocking failure prevents an ACME request; an
advisory is reported but does not make issuance impossible.

### `Workflow`

Crash-visible progress for work that crosses transaction systems. Required
fields are `id`, `kind`, `status`, `operator`, `created_at`, and `updated_at`.
`resource_id` and `error` are optional; `steps` defaults to `[]`.

### `WorkflowKind`

`deployment`, `rollback`, `certificate`

### `WorkflowStatus`

`pending`, `running`, `succeeded`, `failed`, `needs_review`

### `WorkflowStep`

Contains required `name` and `completed_at`, plus an operation-specific
`details` object.
