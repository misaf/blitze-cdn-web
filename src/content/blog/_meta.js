/*
 * Sidebar order for the blog. Posts are listed on the index page newest-first
 * from each post's `date` frontmatter (see `src/components/post-list.jsx`);
 * this file only controls the sidebar, so a new post needs adding here as well
 * as on disk.
 */
export default {
  index: 'All posts',
  'a-version-is-an-interface': 'A version is an interface',
  'one-lock-two-halves': 'One lock, two halves',
  'the-reference-writes-itself': 'The reference writes itself'
}
