---
title: Blog
description: Notes on how BlitzeCDN is built — the boundaries it enforces, the failures it plans for, and the scope it refuses.
---

import { PostList } from '@/components/post-list'

# Blog

Notes on how BlitzeCDN is built: the boundaries it enforces, the failure modes
it plans for, and the scope it deliberately refuses.

<PostList />
